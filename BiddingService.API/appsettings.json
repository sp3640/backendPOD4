{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  // 1. Database Connection
  "ConnectionStrings": {
    // FIX 1: Removed 'Trusted_Connection=True' because it implies Windows Authentication,
    // which contradicts using 'User Id' and 'Password' (if you were using them).
    // If you intend to use Windows Authentication (recommended for local development):
    "DefaultConnection": "Server=LTIN650334\\SQLEXPRESS;Database=OnlineAuctionBiddingDB;Integrated Security=True;MultipleActiveResultSets=true;TrustServerCertificate=True"

    // --- OR ---

    // If you intend to use SQL Server Authentication (must be used in your previous attempts):
    // "DefaultConnection": "Server=LTIN650334\\SQLEXPRESS;Database=OnlineAuctionBiddingDB;User Id=SA;Password=password-1;MultipleActiveResultSets=true;TrustServerCertificate=True" 
  },

  // 2. Service-to-Service Communication URL
  "AuctionService": {
    "Url": "http://localhost:5201/"
  },

  // 3. JWT Configuration (Must match Identity and Auction Services)
  "Jwt": {
    "Key": "ThisIsASecretKeyForOnlineAuctionSystemJWTForCore8",
    "Issuer": "OnlineAuctionIdentityService",
    "Audience": "OnlineAuctionSystemClients",
    "ExpireDays": 7
  }
}